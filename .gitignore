# ── secrets & private state — NEVER commit ──────────────────────────────
# Filled env files (the templates *.env.example are the only ones tracked)
relay.env
.env
**/.env
!**/.env.example
# VAPID / TLS private keys
*.pem
*.key
# Runtime data: the conversation DB and uploaded attachments are private
relay.db
relay.db-*
uploads/
inbox/
last_in_id

# ── build / deps ────────────────────────────────────────────────────────
node_modules/
bun.lock
bun.lockb
__pycache__/
*.pyc
venv/
.venv/

# ── editor / OS noise ───────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.bak
*.bak-*
.idea/
.vscode/

# workspace temp uploads (screenshots / paste-in files for analysis)
.uploads/

# ── 本地设计/分析临时产物（不进仓库）────────────────────────────────────
.design_extract/
.obref/
.trae-html-share-packages/
modref/
ombre-brain/
pwa_preview_shots/
.*.mjs
111.jpg
222.jpg
微信图片_*.jpg
Cat Valley*.zip
登录*.zip
荧光海滩背景.png
首页.png
api_loop_normalize.patch
